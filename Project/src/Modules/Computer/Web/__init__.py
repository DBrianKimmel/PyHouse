"""
This is a web service package.
it implements the web "service" of a the PyHouse twisted application.


New functions may be added to the House Menu, the Computer Menu, or the Root Menu.

In one of the three menu JS files, add a line for the new button/function.

"""

__version_info__ = (1, 6, 0)
__version__ = '.'.join(map(str, __version_info__))

# ## END DBK
